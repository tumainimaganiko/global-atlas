import { createSlice } from "@reduxjs/toolkit";

const initialState = {
    details: []
}
const detailsSlice = createSlice({
    name:details,
    initialState,
    reducers:{},
    extraReducers:{}
})